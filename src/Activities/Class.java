package Activities;

import java.time.LocalDate;

public class Class extends IActivity {
    public Class(int id, String title, LocalDate start, LocalDate end) {
        super(id, title, start, end);
    }
}
