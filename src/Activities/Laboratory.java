package Activities;

import java.time.LocalDate;

public class Laboratory extends IActivity {
    public Laboratory(int id, String title, LocalDate start, LocalDate end) {
        super(id, title, start, end);
    }
}
